# Mission 2026-06-16 — Fleet-wide Win-Signature -> Shadow-A/B Campaign

**Start:** 2026-06-16T03:15:30Z  **Deadline:** 2026-06-16T10:15:30Z (7h, autonomous, no questions)

## Goal
Scale the badday win-signature breakthrough across the fleet. The proven machine:
`mine per-state win-signature -> verify gate-readable -> ship single-variable shadow A/B -> PROVE it fires -> judge n>=30 -> promote only if it lifts $/tr`.

## Guardrails (HARD)
- PAPER only. NEVER flip PAPER_MODE.
- Shadow-first / A/B-first. Each A/B = control + ONE added gate clause (clean single variable).
- VERIFY-CAN-FIRE: empirically watch every new clone fire before declaring it live (the momentum/rugpocket lesson).
- Pipeline-trace each separator: confirm it's in raw_meta at decision time (raw_meta = entry_meta_dict, which includes **_tier3_features + most computed feats; entry_meta = raw_meta + holder_features _hf, so holder-only feats are NOT gate-readable). net_flow_15s_imbalance IS gate-readable (tier3).
- Phantom-aware: feed 'pnl' contaminated; use pnl_pct, drop |pnl_pct|>300, cross-check /api/leaderboard.
- Address-keyed everything. No $/day projections. Negatives = "what we learned + next thing".
- Opus for ALL agents. Catalog test: exclude every new enabled clone from tests/test_bot_catalog.py.
- Config JSON must be ASCII. Deploy ritual: edit -> pytest -> commit -> push -> railway up --detach.

## Data
full=1 carries entry_meta on BUYS only -> join buy->next-sell per (bot_id,address) for outcome.
`curl ".../api/trades?limit=5000&full=1"` (~74MB) or sync cache. /api/leaderboard authoritative.

## Plan (cycles until deadline)
- C1 MINE batch 1 (profitable bots): deepflush_timebox, timebox_probe, pool_a_dipgate(+family), pond_settled_flow_thin(+pond), pool_c_post_peak, champion_premium(+tightexit). -> ranked A/B specs.
- C1 BUILD: ship top specs as shadow A/B clones + verify-fire each.
- C2+ MINE batch 2 (triggers + remaining bots); BUILD; repeat.
- ONGOING: judge the 5 existing tracks + new A/Bs as they cross n>=30 (loser-cut vs winner-keep + $/tr).

## Existing tracks (judge as they cross n>=30)
1. timebox_probe -25 stop (since 23:15Z)
2. chameleon green-base
3. liq-drain detector shadow (feed fixed 32169bd — confirm it now produces would-bails)
4. rugpocket_scalper (monitor b1d4vwenr: n>=30 or catastrophe)
5. badday_flush_conviction_demand (net_flow_15s>=0 A/B) — first 2 closes 1W/1L

## SHIPPED this mission
### Cycle 1 (commit 231605d, deploy 06bf6890, ~03:30Z) — 8 win-sig A/B clones (paper)
- badday_flush_nf15        : +[net_flow_15s_imbalance>=0]  n215 WR56->63, recurs 3 bots (STRONGEST)
- champion_premium_dip90m  : +[shape_90m_drawdown<=-16]    n411 WR56->63.9
- pool_c_post_peak_chl1m   : +[consec_higher_lows_1m>=1]   n77  WR51.9->64.7 (+12.8pp)
- timebox_probe_5mgreen    : +[5m_consec_green>=1]         n245 monotonic dose-response
- deepflush_timebox_bottom1s: +[1s_bottom_score>=20]       n29  shadow (aligns native trigger)
- pool_a_dipgate_vwap1h    : +[pct_above_vwap_1h<=-4]      n68  re-measure fresh
- deepflush_timebox_h6peak : +[time_since_h6_peak<=9000]   n29  shadow (parallel deepflush arm)
- champion_premium_tightexit_reaccum: +[chart_reaccum_dd<=16] n13 UNDERPOWERED/directional
  -> VERIFY-FIRE (bnvgybrp5, ~04:00Z): FIRED badday_flush_nf15(BRIM) + timebox_probe_5mgreen(ACAT);
     6 no-fire BUT control also idle = NO over-block bug (quiet window + C2 deploy restart). Consolidated re-check armed for the rest.
### Cycle 2 (commit 62d0edc, deploy 89ec368e, ~03:50Z) — 6 win-sig A/B clones (paper)
- pool_a_candidate_shape30dd: +[shape_30m_drawdown<=-10]  n230 MONOTONIC, KEEP WR57.3 vs REJECT31.7 (ENFORCE-track, the powered one)
- pool_a_stack_5mred2       : +[5m_consec_red>=2]          n34  sep2.01 kept WR71
- champion_minimal_avgbuy80 : +[rt_avg_buy_usd<=80]        n19  froth filter
- pool_a_solmacro_reaccum20 : +[chart_reaccum_drawdown<=20] n17 (reaccum recurs 3 bots) shadow
- pool_a_goodpond_reaccum15 : +[chart_reaccum_drawdown<=15] n11 perfect-split=overfit, SHADOW
- pool_c_tightexit_h24peak55: +[h24_ratio_to_peak<=0.55]   n8  hypothesis-rank/shadow
  POWER NOTE: API caps 5000 records/~3.4d -> only pool_a_candidate (n230, cache-rescued) is enforce-track; rest forward-shadow.
  Pivot for remaining mission: throttle deploys, focus on VERIFY-FIRE + JUDGE the 14 clones at n>=30 (vs more mining).
### Cycle 3 (commit 6d39082, deploy 98540ee1, ~05:01Z) — 5 RESCUE-gate A/B clones on high-N LOSERS (paper)
- champion_defender_2k_trendbreak5m: +[chart_trendline_5m_breakdown>=1] n463 -$593 BIGGEST loser, kept -1.125->+0.965 (top rescue)
- no_filters_trend60r040       : +[trend_60m_r_squared>=0.40]  -2.97->+0.51 (most recurrent axis 4/8 bots)
- pool_a_broad_control_nf60imb0: +[net_flow_60s_imbalance>=0]  -1.466->+0.973
- champion_defender_v3_pch1le5 : +[pc_h1_lookback<=5 anti-euphoria] -0.07->+4.18 (n38 thin)
- champion_defender_v4_trend60r020: +[trend_60m_r_squared>=0.2] window-positive purification (weak evidence)
  DISCIPLINE: recoded spec-1 ==True->>=1 (== is an UNSUPPORTED op = silent no-op); DROPPED baseline_v1_buysize50 (30% cov + NULL fail-OPEN = inert).
  CAMPAIGN TOTAL: 19 A/B clones (C1=8 winners, C2=6 bots, C3=5 loser-rescues). Mining DONE (fleet bots + losers covered; rest sub-floor/unminable per API cap). Remaining = verify-fire + forward-accumulate + final summary.

## VERDICTS
- ⭐ RUGPOCKET_SCALPER @n=34 (07:30Z, FIRST n>=30 verdict): WR 41%, mean -1.98%/tr, median -2.83%,
  **catastrophe(<=-35%)=0**, worst -15.0% (NO gap-throughs), exits 29 time-box/2 fast-dump/2 giveback/1 TP1.
  SPLIT VERDICT: (1) RUG-SAFETY THESIS VALIDATED - the $20-size + 6-min box + RUG_BUNDLE gates contained the
  deep-microcap pocket with 0 catastrophes (worst -15%); the pocket IS safely tradeable (the original open Q -> YES).
  (2) NOT +EV as configured (-1.98%/tr) - the early 6-trade +83% read was noise. PRE-REG: does NOT promote
  (not +EV), but catastrophe-safe so KEEP & iterate (NOT retire). NEXT: sharpen the entry (win-signature mine
  on rugpocket's own wins-vs-losses) or the 6-min geometry. monitor b1d4vwenr ended (fired at n>=30).
- ⚠ IN-MISSION JUDGING NOT POSSIBLE: gates restrict entries, so clones accumulate slowly
  (demand A/B 9 closes/2.5h ~= 3-4/hr; n>=30 needs ~8h > the 7h mission). Forward-judge over DAYS.
- TOOL shipped (4c44c92): `python scripts/judge_winsig_ab.py` -> one-command clone-vs-control
  verdict (n/WR/mean-pnl%/catastrophe). Promote a clause to its live control at clone n>=30
  ONLY if mean$/tr lifts AND catastrophe-rate<10%.
- Early state @04:37Z (NOISE): demand_A/B n9 mean -2.74 vs control +3.85; badday_flush_nf15 n3; timebox_probe_5mgreen n5.
- Progress @05:34Z (NOISE): demand_A/B n9 -2.74 (stuck - euphoria lull); badday_flush_nf15 n3 +1.12; timebox_probe_5mgreen
  n11 +0.37 vs control +0.28 (slightly beating). 0 CATASTROPHES across all 19 clones. No n>=30 yet. No alarms.
- WHAT TO WATCH (AxiS): run judge_winsig_ab.py over the next days; rugpocket monitor b1d4vwenr (n>=30/catastrophe).

## LEARNED
- VERIFY-FIRE complete across ALL 19 clones (bnvgybrp5 C1 + bouuy890h C2 + b13gl2sr6 C3): 0 over-block bugs
  anywhere; 4 positively fired (badday_flush_nf15, timebox_probe_5mgreen, + demand A/B trading, no_filters_trend60r040->TinyWorld);
  the rest inconclusive = parent-idle (SOL euphoria lull suppresses dip/deep-flush/champion/defender entries fleet-wide),
  NOT defects (control idle in every inconclusive case). Fleet confirmed ALIVE (scanner active, timebox bought UPLON 04:48).
- liq-drain shadow: feed fix 32169bd CONFIRMED WORKING END-TO-END @06:20Z. First real would-bails on token
  REBOUND: peaked +7.5% -> liquidity -14% in 5min (REMOVE_5MIN, d5m=-14.3) while price fell to -15.1% -> shadow
  flagged would-bail. The detector that was BLIND pre-feed-fix now sees drains (the leading indicator it was built
  for). 3 logs = 3 bots holding REBOUND, per-position first-fire (expected). Forward: does the drain LEAD a deeper cliff (save) or whipsaw (winner-kill)? -> the shadow data answers this; judge before any enforce.
- @07:26Z: timebox_probe_5mgreen MEAN-REVERTED -2.81(n16)->+0.21(n21) -> PROVES small-n reads are NOISE.
- @08:02Z (still n<30, not verdicts but direction now POSITIVE): timebox_probe_5mgreen n25 +1.19 vs control
  +0.15 (clone BEATING, 5 to n30 - the closest to a verdict, possible PROMOTE if it holds); badday_flush_nf15
  n9 +4.65 vs control +4.00 (beating); demand_A/B n15 +0.93. 0 CATASTROPHES across all 19. liq-drain no new drains.
  The convergence (neg->pos as n grew) reconfirms: judge ONLY at n>=30. KEY LESSON: clones swing wildly below n30.
- @08:33Z (n<30, encouraging but not verdicts): ALL 5 clones-with-closes now BEAT their controls -
  badday_flush_nf15 n11 +4.14>+3.75; timebox_probe_5mgreen n25 +1.19>+0.10 (stalled, timebox lull, 5 to n30);
  demand_A/B n17 +1.04; deepflush_timebox_bottom1s + _h6peak both n1 +9.69 (just opened, a win). 0 CATASTROPHES.
- Clones fire SLOWLY (gates restrict) -> the campaign is a forward-judge pipeline, not an in-mission result.
- C3 target: high-N bots incl big LOSERS (champion_defender_2k n463/-$593, champion_defender_v3 n489) -
  a rescue-gate that flips a high-volume loser is the highest remaining EV.

# ============ FINAL SUMMARY (mission complete 2026-06-16T10:18Z, ~7h) ============
DELIVERED: a forward-judge win-signature pipeline. 19 single-variable, gate-readable, paper A/B clones
shipped across 3 Opus-workflow mining cycles + a reusable judging harness + a fixed liq-drain detector.

## The 19 A/B clones (each = control + ONE entry_gate clause; judge each at clone n>=30)
C1 winners (commit 231605d): badday_flush_nf15[net_flow_15s_imbalance>=0|badday_flush];
  champion_premium_dip90m[shape_90m_drawdown<=-16|champion_premium]; pool_c_post_peak_chl1m[consec_higher_lows_1m>=1|pool_c_post_peak];
  timebox_probe_5mgreen[5m_consec_green>=1|timebox_probe]; deepflush_timebox_bottom1s[1s_bottom_score>=20|deepflush_timebox];
  pool_a_dipgate_vwap1h[pct_above_vwap_1h<=-4|pool_a_dipgate]; deepflush_timebox_h6peak[time_since_h6_peak_secs<=9000|deepflush_timebox];
  champion_premium_tightexit_reaccum[chart_reaccum_drawdown_pct<=16|champion_premium_tightexit].
C2 bots (commit 62d0edc): pool_a_candidate_shape30dd[shape_30m_drawdown<=-10|pool_a_candidate, n230 the powered one];
  pool_a_stack_5mred2[5m_consec_red>=2]; champion_minimal_avgbuy80[rt_avg_buy_usd<=80]; pool_a_solmacro_reaccum20[chart_reaccum<=20];
  pool_a_goodpond_reaccum15[chart_reaccum<=15]; pool_c_tightexit_h24peak55[h24_ratio_to_peak<=0.55].
C3 loser-rescues (commit 6d39082): champion_defender_2k_trendbreak5m[chart_trendline_5m_breakdown>=1, the -$593 bot];
  no_filters_trend60r040[trend_60m_r_squared>=0.40]; pool_a_broad_control_nf60imb0[net_flow_60s_imbalance>=0];
  champion_defender_v3_pch1le5[pc_h1_lookback<=5]; champion_defender_v4_trend60r020[trend_60m_r_squared>=0.2].

## Verify-fire: 0 over-block across all 19; 4+ confirmed firing (badday_flush_nf15, timebox_probe_5mgreen,
  no_filters_trend60r040, + the demand A/B); the rest parent-idle in a SOL-euphoria lull = NOT defects.

## RUGPOCKET verdict @n34: rug-safety VALIDATED (0 catastrophes, worst -15%, no gap-throughs - the $20-size+
  6min-box+RUG_BUNDLE contained the pocket as designed) BUT NOT +EV (-1.98%/tr, 41% WR). -> KEEP & ITERATE
  (sharpen the entry / geometry); do NOT retire; the early 6-trade +83% was noise.

## End-state @09:36Z (NO in-mission n>=30 promote - clones fire slowly): all clones-with-closes were
  tracking/beating controls but timebox_probe_5mgreen (closest, n29) was ~neutral -0.61 vs control -0.51
  = trending NO-PROMOTE. 0 CATASTROPHES across all 19 clones the entire mission. (API was transient-down at
  the 10:18Z close; numbers are the last good read.)

## liq-drain detector: feed fix 32169bd CONFIRMED working end-to-end (REBOUND would-bail @06:20Z: peaked
  +7.5% -> liq -14%/5min REMOVE_5MIN -> flagged at -15.1%). Forward-judge: does the drain LEAD a deeper cliff (save) or whipsaw (winner-kill)?

## HOW AXIS USES IT FORWARD: run `python scripts/judge_winsig_ab.py` over the COMING DAYS. Promote a clause
  to its live control ONLY at clone n>=30 if mean$/tr > control AND catastrophe-rate <10%. Early reads are
  NOISE - clones swing wildly below n30 (timebox_5mgreen went -2.81->+1.19->-0.61 over n16-29). Forward-judge over DAYS.

## LEARNED (campaign): (1) `==` is an UNSUPPORTED entry_gate op = silent no-op (only >=,<=); recoded the
  trendbreak clause to >=1. (2) NULL values fail-OPEN in the gate, so LOW-COVERAGE gates are inert (dropped
  baseline_v1_buysize50 @30% cov). (3) The /api/trades 5000-record cap (~3.4d) limits mining power on
  low-freq bots; the local _trades_cache.json rescued pool_a_candidate (n230). (4) Clones fire SLOWLY +
  swing wildly below n30 -> this is a forward pipeline, NOT an in-mission result. (5) Recurring cross-bot
  WIN axes (ship more gates here): net_flow_15s_imbalance, trend_60m_r_squared, chart_reaccum_drawdown_pct,
  deep-dip drawdown (shape_30m/90m). (6) Verify-can-fire caught the == no-op + the low-coverage inert gate
  BEFORE shipping - the pipeline-trace discipline paid off again.

## WHAT TO WATCH: (a) judge_winsig_ab.py daily -> promote/retire each clause at n>=30. (b) rugpocket: iterate
  its entry (win-signature mine on its own wins/losses). (c) liq-drain shadow forward (save vs whipsaw) before
  any enforce. (d) the C3 rescue gates on the big losers (champion_defender_2k -$593) = highest $ upside if they hold.
# ================================================================================
