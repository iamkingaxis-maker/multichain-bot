# Railway-only ignore for deploy uploads. Does NOT affect git.
# 2026-05-28: working-copy bloat hit 223MB; Cloudflare 413 rejected upload.
# Exclude scratch/analysis directories and large data dumps.

# Mining/analysis scratch directories (all dot-prefixed working dirs)
.mining_*/
.winloss_8hr/
.watch7h/
.chart_analysis/
.chart_analysis_round2/
.deep_mine_cache/
.perf_diff_drafts/
.claude/

# Large data dumps (trades pulls, candle data, training sets)
.perf_diff_trades*.json
.perf_diff_completed_paired.json
.perf_diff_fresh.json
.perf_diff_trades_older.json
.trades_*.json
.candle_*.json
.burnie_*.bin
.cnn_dataset/
.cnn_models/
# Non-dot-prefixed trade dumps + scratch pulls at repo root
trades_*.json
.t_*.json
.hist_trades_*.json
.all_trades_*.json
ep_*.json
.deep_mine_*.json

# Per-session output files
.*_results.json
.*_output.txt
.*_check.txt
.*_logs.txt
.*_logs2.txt

# Backup files
*.bak
*.backup
*.orig

# Python cache
__pycache__/
*.pyc
.pytest_cache/

# OS noise
.DS_Store
Thumbs.db

# 2026-05-30: workspace cleanup archive + session scratch (38MB, deploy-only exclude)
_archive/
.fresh_launch_dataset.jsonl
.watch_log.jsonl
.watch_loop.out
.watch_snapshots.json
.uni_raw.json

# 2026-06-21: underscore-prefixed session scratch (mining/decode/watch outputs,
# trade-pull dumps). These are working scratch at the repo root and must NEVER
# upload — multi-GB of them accumulated (513MB/486MB/482MB + many 70MB pulls)
# and would 413 the deploy. None are runtime code (start command is python main.py;
# runtime data lives in DATA_DIR, a volume mount — not these).
_*.json
_*.json.gz
_*.jsonl
_*.txt
_*.out
_*.err
.mission_trade_marker
_full_trades.json
_df_full.json.gz
_df_trades.json.gz

# 2026-07-11: scratchpad hit 1.6GB (rh_history backfill + tapes) -> uploads
# 413'd again (FAILED build de78fe31 + silent up errors). Never upload scratch.
scratchpad/
_*.txt
_*.json
_*.json.gz
scratch_*.py
scratch_*.txt
out.txt

# 2026-07-13: today's analysis dumps (sync cache + agent fan-out) bloated the
# working tree to 12G — railway up would 413. All local analysis scratch, never
# read by the deployed service. Root-anchored so package files are unaffected.
/analysis/
/_trades_cache.json
/_*.json
/_*.json.gz
/_*.txt
/out.txt
/scratch_*.txt
/scratch_*.py
/*_full.txt
/*_out.txt
/*_err.txt
# Hidden root-level data dumps (.hist_trades, .tmp_*, .uni_*, .now.json, etc.)
/.*.json
/.*.txt
